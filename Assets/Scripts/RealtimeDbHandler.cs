
public class RealtimeDbHandler
{
    /*
    private FirebaseApp app;
    private DatabaseReference dbReference;
    
    public RealtimeDbHandler()
    {
      InitializeFirebase();
    }
    
    // Initialize the Firebase database:
    protected virtual void InitializeFirebase() {
        app = FirebaseApp.DefaultInstance;
        // NOTE: You'll need to replace this url with your Firebase App's database
        // path in order for the database connection to work correctly in editor.
        app.SetEditorDatabaseUrl("https://locked-3426c.firebaseio.com/");
        if (app.Options.DatabaseUrl != null) app.SetEditorDatabaseUrl(app.Options.DatabaseUrl);

        dbReference = FirebaseDatabase.DefaultInstance.GetReference(FirebaseAuth.DefaultInstance.CurrentUser.UserId+"/levels/");
    }

    private void saveLevel()
    {
        
    }
    */
}